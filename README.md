# MongoDB Connection (Python)

This project connects to MongoDB using the Python driver **PyMongo**.

## Prerequisites

- Python 3.8+
- MongoDB (local or MongoDB Atlas)
- PyMongo

Install PyMongo:
```bash
pip install pymongo
